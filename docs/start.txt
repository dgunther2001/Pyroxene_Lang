MIT License
Copyright (c) 2024 Daniel Gunther

For the full license text, see the LICENSE.txt file in the docs directory.
If LICENSE.txt is not included, this version of the source code is provided in breach of this license.

******************************************************

If you run MacOS or any Flavor of Linux (Bash and Zsh Shells):

    How to run the language without adding aliases to your shell configuration file
        => go into project root directory
        => for debug mode, type:
            chmod u+x run_debug_driver.sh
            ./run_debug_driver.sh /path/to/your/pyroxene/file
        => for the regular language driver, type:
            chmod u+x run_driver.sh
            ./run_driver.sh /path/to/your/pyroxene/file

    Add aliases to your shell configuration
        => go into project root directory
        => add permissions to shell scripts
            chmod u+x run_debug_driver.sh run_driver.sh setup.sh
        => run setup file and allow alias creation (if it asks you to install Docker, refer below based on your OS, or install Docker Desktop)
            ./setup.sh
        => reload shell settings to allow alias to be used
            - in zsh
                => source ~/.zshrc
            - in bash
                => source ~/.bashrc
        => run regular language driver
            pyroxene /path/to/your/pyroxene/file
        => run in debug mode
            pyroxene-debug /path/to/your/pyroxene/file

If you run on Windows using Powershell or Cmd.exe:
    (INSERT WINDOWS INSTRUCTIONS AND TEST IN AWS VM)

Installing Docker in Ubuntu:
    => sudo apt-get update
    => sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common
    => curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
    => echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
    => sudo apt-get update
    => sudo apt-get install -y docker-ce docker-ce-cli containerd.io
    => sudo systemctl start docker
    => sudo systemctl enable docker